### 00:00
<!-- session:019f645b-f71a-70f0-aaea-79e7b3ba413b rollout:/Users/jon/.codex/sessions/2026/07/14/rollout-2026-07-14T22-59-36-019f645b-f71a-70f0-aaea-79e7b3ba413b.jsonl -->
- User asked: the url shpould be up now.
- Codex: Confirmed: `https://collector.agentburndown.com` is now live.

- DNS resolves through CloudFront.
- TLS certificate validates.
- `/api/health` returns HTTP 200.
- All three ingest routes respond with the expected HTTP 401 when tested without credentials, confirming they’re routed correctly.

The installed client is still configured for `https://app.agentburndown.com`, and its daemon is currently stopped. It has not switched to the new endpoint yet.

### 00:07
<!-- session:019f645b-f71a-70f0-aaea-79e7b3ba413b rollout:/Users/jon/.codex/sessions/2026/07/14/rollout-2026-07-14T22-59-36-019f645b-f71a-70f0-aaea-79e7b3ba413b.jsonl -->
- User asked: do we need to update the client now?
- Codex: Yes. The collector endpoint is healthy, but the client still uses `https://app.agentburndown.com`.

The update should:

- Change the compiled API default to `https://collector.agentburndown.com`.
- Migrate existing configs only when `api_url` equals the old default, preserving custom URLs.
- Keep dashboard/sign-in links pointing to `app.agentburndown.com`.
- Release/reinstall and restart the daemon.

One related backend issue: device login through the collector hostname works, but it currently returns an AWS `execute-api` URL for activation. That verification URL should also use the intended public hostname.

Existing collector keys should remain valid because the new hostname routes to the same API. Your installed daemon is currently stopped, so it will need restarting after migration.
